# Ontological Debt Ledger

This ledger tracks human-supplied meta-assumptions across the numbered
mathematical ladder. Debt is reported independently from milestone success; it
does not create replacement milestones or reorder M1–M30.

| ID | Supplied assumption | First explicit audit | Status before M15 |
| --- | --- | --- | --- |
| D1 | Structural lifting: a scalar program may be lifted pointwise over a structured object. | M14/M14c | supplied |
| D2 | Probe mechanism: interventions may be chosen and their responses compared to route concepts. | M13b/M14c | supplied |
| D3 | Stability preference: representations with simple transformation response are valuable. | M14 | supplied |

Every subsequent boundary must state whether it uses, weakens, eliminates, or
adds debt. A milestone may succeed while leaving all debt intact. Debt may only
be marked eliminated when the relevant mechanism is constructed and retained
from a weaker frozen substrate, with ablations and downstream utility.

## M15 entry (pre-registered)

- D1 is weakened but not eliminated: recurrence execution over a sequence is
  supplied, while oscillatory coordinate sequences and their combinations are
  not.
- D2 is not used for candidate routing; fixed train/held-out signal splits are
  supplied instead.
- D3 remains supplied: simple cyclic-shift dynamics, sparse reconstruction,
  prediction, and composition are explicit objectives.
- New debt D4 is added: bounded second-order linear recurrence is supplied as a
  generic generator meta-language. Named frequencies, trigonometric atoms,
  complex exponentials, orthogonality, projection, and Fourier transforms are
  not supplied.

## M15 outcome

D4 remains supplied. M15 shows that the language can construct exact useful
oscillatory coordinates, but does not eliminate recurrence generation. D3 also
remains: simple dynamics is useful descriptively, yet unconditional M9-guided
priority produces negative transfer on three of five tasks. This is evidence
that applicability of the stability preference is unresolved, not evidence for
removing the debt entry.
