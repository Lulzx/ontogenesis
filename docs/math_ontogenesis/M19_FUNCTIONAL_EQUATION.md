# M19 — Invent a Functional Equation

## Required discovery

From observations in different parts of a toy zeta object's domain, the system
must discover both a reflection center and an auxiliary transformation relating
the regions, such as `F(s)=factor(s)F(c-s)`.

## Status: reached — exact L3 toy functional equation

M18 supplied the toy completed object `Xi(s)=prod_p(p^{1-s}-p^s)`. M19
provides exact values of `Xi` in two regions and hides the center and
transformation. From a bounded affine-involution grammar and a factor grammar
over the inferred irreducible count `k`, the learner retains `T(s)=1-s` and
`F(k)=(-1)^k`, giving `Xi(1-s)=(-1)^k Xi(s)`.

On three frozen unseen prime sets (with `k=5`, `k=3`, `k=3`), the identity is
exact on all held-out reflection pairs and operations fall 212→148.
Asymmetric-universe and corrupted-`Xi` controls are declined; description cost
falls 72→14 integers. M19 reaches
`L3_transferred_ontology_with_measured_utility` as a bounded exact toy
functional equation. The affine grammar, power primitive, completed-object
definition, and exact rational checker remain supplied; no analytic
continuation or general functional-equation theorem is claimed.

## Completion criteria

Search must hide the transformation and center, compare candidate symmetries by
total theory compression, verify the identity symbolically or within a trusted
finite toy algebra, and include asymmetric controls. The discovered relation
must reduce extrapolation/reasoning cost outside the fitting region.
