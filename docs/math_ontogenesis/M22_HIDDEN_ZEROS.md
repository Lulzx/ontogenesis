# M22 — Infer Hidden Zeros From Arithmetic

## Required discovery

Zeros are withheld. From counting functions, errors, or arithmetic
oscillations, the system must invent latent oscillators, complex exponents, and
the corresponding hidden spectral locations.

## Status: reached — exact L3 hidden toy zeros

M18–M21 now supply that toy analytic world. M22 withholds zero positions and
supplies exact arithmetic signals `a[t]=sum w_u q(u)^t` over the toy lattice.
The learner searches a bounded oscillator dictionary, retains the hidden
oscillator set `u in {-2,0,3}` with weights `{1,2,1}`, and completes each
recovered frequency through the M21 diagonal locus to `(u,1-u)`.

Three frozen unseen hidden sets are recovered exactly and lattice evaluations
fall 6,897→4,779. Corrupted-signal, off-locus, and asymmetric-universe
controls are declined; description falls 51→12 integers. M22 reaches
`L3_transferred_ontology_with_measured_utility` as a bounded integer toy. The
oscillator dictionary, weight range, exact checker, and retained locus remain
supplied; no complex-frequency estimation or spectral inversion is claimed.

## Completion evidence

A future protocol needs blinded synthetic arithmetic observables, model-order
selection, complex-frequency inference, reconstruction of held-out
oscillations, and comparison to non-spectral baselines. Identifiability limits
and aliasing must be reported. Feeding zeros into feature construction or
fitting frequencies only after inspecting them would invalidate the claim.
