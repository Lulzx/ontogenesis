# M27 — Rediscover the Critical Line

## Required discovery

From a machine-discovered completed zeta object and finite zero/arithmetic
information, M27 must formulate the simplest structural conjecture equivalent
to `Re(rho)=1/2` for nontrivial zeros. Critical strip, critical line, and RH are
not supplied.

## Status: M27a invalidated; M27b reached — finite-data L2 conjecture

M26 has selected and validated a completed object. M27 freezes independent
two-dimensional numerical root recovery, an unlabeled integer quadratic locus
language, cold and ontology-informed ordering, disjoint height bands, and
negative controls before execution.

## Evidence required

The locus grammar and simplicity score must predate the data; trivial zeros
must be separated without using the target label; zero and arithmetic evidence
must be independently held out; and alternative loci must be scored. The
result remains a conjecture regardless of how many finite zeros agree.

The result is capped at L2 by D17/D18. Root finding, candidate coordinates,
the locus grammar, simplicity score, reflection-fixed-set preference, and
validation protocol are supplied. No passing outcome may be described as a
proof of RH.

## M27a audit

M27a recovered 3 training and 7 held-out roots, selected `2x-1=0`, and reduced
candidate tests 18→8. A post-run audit invalidated the claim gate: two controls
were extensionally the same translated zero set, and the algebraic-factor
control required only one of `s=0,1` to be rejected. The result is preserved
but not counted as reached. M27b freezes corrected controls in
`NEXT_BOUNDARY.md`; all search and numerical settings remain unchanged.

## M27b result

The corrected run recovered three roots in the training height band and seven
roots in the disjoint higher band from two-dimensional seed grids. From 58,095
normalized integer quadratic loci, cold and transferred searches both selected
`2 Re(rho)-1=0`; candidate tests fell 18→8. Increased-work recovery, held-out
zeros, conjugates, a primary falsifier, and all four corrected controls passed.

The machine record states `proof=false`. This is an L2 conjecture selected in
a supplied numerical and locus meta-ontology. It does not establish that all
nontrivial zeta zeros satisfy the locus.
